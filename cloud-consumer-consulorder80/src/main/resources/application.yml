server:
    port: 80

spring:
    application:
        name: cloud-consumer-consul-order
    cloud:
        consul:
            host: hadoop000
            port: 8500
            discovery:
                #hostname: 127.0.0.1
                service-name: ${spring.application.name}
